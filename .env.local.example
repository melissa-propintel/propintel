# PropIntel environment — copy to .env.local and fill in.
# Nothing here is required for the v1 report spine (intake -> report -> PDF runs fully local).
# These wire up the later intelligence + persistence layers.

# Anthropic (Session 4-5: AI doc parser, narrative enrichment)
ANTHROPIC_API_KEY=

# Supabase (persistence, auth, lender portal — later phases)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
